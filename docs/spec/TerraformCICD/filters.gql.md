# Filters

Having the ability to filter on various fields for the entities in the Terraform CICD service
ecosystem could lend itself useful to a user, especially if they're wanting to narrow their search results.

For filtering we are using Hot Chocolates default filter implementation.  The following filters are supported.

<!--Add the GraphQL filters here.-->
